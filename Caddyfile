:8080

# Carpeta pública de Laravel
root * /app/public

# Servir archivos estáticos (CSS, JS, imágenes)
file_server

# Enviar todas las solicitudes PHP a FrankenPHP
php_fastcgi unix//tmp/php-fpm.sock

# Manejar URLs amigables de Laravel
try_files {path} {path}/ /index.php?{query}
