[phases.setup]
nixPkgs = ["php82", "php82Extensions.mysqlnd", "nginx"]

[phases.build]
cmds = [
  "composer install --no-dev --optimize-autoloader"
]

[start]
cmd = "php-fpm -F & nginx -c /workspace/nginx/default.conf -g 'daemon off;'"
